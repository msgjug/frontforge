var Macro = {
    APP_NAME: "{{APP_NAME}}",
    APP_VERSION: "0.2",
    FRAME_VERSION : "2.1",
    APP_VERSION_TIMESTAMP: "",
    HTTP: {
        PATH: "{{PATH}}",
        DOMAIN: "{{DOMAIN}}",
        APIS: {
            REQ: "req.do",
        }
    }
};

export default Macro;